#version 330 core

layout (points) in;
layout (points, max_vertices = 1) out;

uniform mat4 ProjectionMatrix;

in vec2 TexToGeom[];

out vec2 TexToFrag;

void main()
{
    TexToFrag = TexToGeom[0];
    gl_Position = ProjectionMatrix * gl_in[0].gl_Position;
    EmitVertex();

    EndPrimitive();
}